# Audio is streamed by a Lavalink node, so the bot itself needs no voice
# extras: no PyNaCl (Opus encryption), no FFmpeg, no yt-dlp.
discord.py>=2.7.0
wavelink>=3.5.2,<4
google-api-python-client>=2.140.0
python-dotenv>=1.0.1
